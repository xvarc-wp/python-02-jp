# 以下ではすべての論理演算子(and、or、not)を使ってください。
# ブール値を保持する変数を作成し、それらを組み合わせて
# 以下の文を表現します。
#
#   誤り(wrong)に誤りを重ねたら正しく(right)なるか?
# 
# この文自体が正しいかどうかは、気にしなくてもかまいません。
# ただし、1行のコードで3つの論理演算子をすべて使用し、
# 結果を別のブール値にしてみてください。

wrong = False
right = True
